import React, { Component } from 'react';
import st from './index.scss';

export default class UI extends Component {
	render() {
		return (
			<div className={ st.UI }>
			</div>
		);
	}
};
